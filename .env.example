# Application settings are values in application.properties. Deployment
# settings and secrets are loaded directly from the active profile's AWS SSM
# Parameter Store path; only profile selection remains an environment value.
QUARKUS_PROFILE=development
